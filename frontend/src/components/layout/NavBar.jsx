const NavBar = () => {
    return(
        <div className="fixed top-0 left-0 w-full h-16 m-0 flex items-center justify-between bg-gray-800 text-white px-8">
        <div>GTR Lens</div>

        <nav>
            {/* Links will go here */}
        </nav>

        <div>Login</div>
        </div>
    );
};

export default NavBar;
