# This is a template for production environment variables.
# DO NOT use this for local development.

# --- Container Name ---
CONTAINER_NAME=gtr_production

# --- Django Settings ---
# Generate a new, strong secret key for production!
SECRET_KEY=""
DEBUG="False"
ALLOWED_HOSTS="yourdomain.com,www.yourdomain.com"
CORS_ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# --- Database ---
# These variables should point to your managed production database (e.g., AWS RDS)
DB_USER=""
DB_PASS=""
DB_NAME=""
DB_HOST=""
DB_PORT="5432"
DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# --- AWS S3 ---
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_STORAGE_BUCKET_NAME=""