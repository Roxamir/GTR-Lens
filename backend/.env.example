# Copy this file to .env for local development

# --- Container Name ---
CONTAINER_NAME=gtr_local_test

# --- Django Settings ---
SECRET_KEY="django-insecure-local-test-key"
DEBUG="True"
ALLOWED_HOSTS="127.0.0.1,localhost"
CORS_ALLOWED_ORIGINS="http://localhost:5173"

# --- Database Configuration ---
# These variables are used by BOTH services:
# 1. The 'postgres' image uses them to create the database.
# 2. The 'DATABASE_URL' variable uses them to connect Django.
POSTGRES_USER=gtr_user
POSTGRES_PASSWORD=gtr_pass
POSTGRES_DB=gtr_db

# This URL is read by Django and automatically uses the variables from above.
# The host is 'db' because that's the name of the service in docker-compose.
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}"